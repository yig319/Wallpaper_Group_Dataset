from setuptools import setup, find_packages

setup(
    name='Wallpaper_Group_Dataset',
    version='0.1',
    author='Yichen Guo, Joshua Agar',
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)
